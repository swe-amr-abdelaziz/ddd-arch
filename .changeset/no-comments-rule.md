---
'eslint-plugin-nestjs-ddd': minor
---

Add the `no-comments` rule: bans comments except tool directives (`eslint*`, `@ts-*`, …) and JSDoc type annotations, so rationale lives in commits and ADRs instead of rotting in the code.
